# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing these this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Manage notes from your command line with CLINotes
email: mail@clinot.es
description: Imagine a world where you would never have to leave your cozy favorite shell envorinment. Sounds pretty awesome, huh? Well, CLINotes does not solve all your problems and in fact it does not solve any problem at all.
baseurl: ""
url: "https://clinot.es"
repository: clinotes/clinotes.github.io

owner:
  name: Sebastian Müller

author:
  twitter: sbstjn

twitter:
  username: sbstjn

# Build settings
markdown: kramdown
theme: minima

gems:
  - jekyll-seo-tag
  - jekyll-sitemap

exclude:
  - CNAME
  - Gemfile
  - Gemfile.lock
  - Makefile
  - README.md

sass:
  sass_dir: 'styles'
  style: 'compressed'
